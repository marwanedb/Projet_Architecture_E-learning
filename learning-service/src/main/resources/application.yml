server:
  port: 8086

spring:
  application:
    name: learning-service
  datasource:
    url: jdbc:mysql://localhost:3306/learning_db?createDatabaseIfNotExist=true
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
